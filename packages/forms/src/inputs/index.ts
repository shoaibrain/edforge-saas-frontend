/**
 * @edforge/forms - Input Components
 *
 * Higher-level archetype-aware inputs that compose multiple primitives.
 * For low-level RHF inputs, see `../fields/`.
 */

export { PhoneInput, buildPhoneInputRules, type PhoneInputProps } from './PhoneInput'
