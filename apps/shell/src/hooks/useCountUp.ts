/**
 * useCountUp — Re-export from @edforge/ui
 *
 * The count-up hook has been extracted to @edforge/ui for use across MFE modules.
 * This file preserves the original import path for backward compatibility.
 */

export { useCountUp, parseFormattedValue, formatAnimatedValue } from '@edforge/ui'
