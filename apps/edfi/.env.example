# API Configuration
# Remotes are "Pure Remotes" that consume the Shell's Authentication context.
# They do NOT need independent Auth configuration.
VITE_API_URL=https://f3xlvrqt24.execute-api.us-east-1.amazonaws.com/prod
