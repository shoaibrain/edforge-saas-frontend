/**
 * Vitest setup for the Finance MFE.
 *
 * Auto-loaded via `vitest.config.ts` → `setupFiles`. Runs once per test
 * file, before any `describe`/`it` blocks. Only put truly global wiring
 * here — per-test mocks belong in their own spec files.
 */

import '@testing-library/jest-dom'
